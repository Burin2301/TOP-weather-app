import {
//   getApiInfo,
  searchCity,
//   marineWeather,
//   showForeCast,
//   showHistory,
} from './apiFunctions';

const contentDiv = document.querySelector('.content');

document.addEventListener('DOMContentLoaded', () => searchCity('bogota'));
